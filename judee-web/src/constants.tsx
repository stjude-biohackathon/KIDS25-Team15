const API_BASE_URL = 'http://localhost:4000/api';

const API_ENDPOINTS = {
  CONTEXT_API: `${API_BASE_URL}/context`,
};

const PAGES = {
  HOME: '/',
}

export { API_BASE_URL, API_ENDPOINTS, PAGES };
